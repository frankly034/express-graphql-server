export const pickProduct = ({
  id,
  name,
  imageUrl,
  description,
  price,
  quantity
}) => ({
  id,
  name,
  imageUrl,
  description,
  price,
  quantity
});

export default {
  pickProduct,
};
